/**
 * redis 集群连接
 */
const redis = require('redis');

class RedisCluster{
    constructor(){
        this.config = [

        ]
    }
    createCluster(){
        return
    }
}
const Execed = require.main === module
console.log(Execed)
console.log(redis.createCluster)